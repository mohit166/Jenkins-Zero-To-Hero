pipeline {
    agent {
        docker { image 'node:16-alpine' }
    }

    stages {
        stage('Hello') {
            steps {
                echo '🟢 Starting Hello World pipeline...'
                sh 'node -v'    // Check Node.js version inside container
                sh 'echo "Hello World from Jenkins running in Docker!"'
            }
        }
    }

    post {
        always {
            echo '✅ Pipeline finished!'
        }
    }
}
